/**
 *
 * 1. Create `UserDetail` type from `User` which contains new fields:
 *    `firstName`, `lastName`, `age`
 * 2. Exclude from `UserDetail` `age` field
 * 3. Write user factory function
 **/
interface User {
  id: number;
  email: string;
}
